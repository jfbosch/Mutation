<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
	x:Class="Mutation.Ui.Views.SettingsDialog"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:Mutation.Ui.Views"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d"
	Title="Settings"
	PrimaryButtonText="Save"
	SecondaryButtonText="Cancel"
	DefaultButton="Primary"
	Width="1480"
	Height="820">

	<ContentDialog.Resources>
		<DataTemplate x:Key="SettingsCategoryTemplate" x:DataType="local:SettingsCategoryItem">
			<StackPanel Orientation="Horizontal" Spacing="12">
				<FontIcon Glyph="{x:Bind IconGlyph}" FontFamily="Segoe Fluent Icons" VerticalAlignment="Center" />
				<TextBlock Text="{x:Bind DisplayName}" Style="{StaticResource BodyStrongTextBlockStyle}" />
			</StackPanel>
		</DataTemplate>
	</ContentDialog.Resources>

	<Grid RowSpacing="24" ColumnSpacing="24">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="260" />
			<ColumnDefinition Width="*" />
		</Grid.ColumnDefinitions>

	<StackPanel Grid.Row="0" Grid.ColumnSpan="2" Spacing="4">
			<TextBlock
				Text="Settings"
				Style="{StaticResource TitleTextBlockStyle}" />
		</StackPanel>

		<Border
			Grid.Row="1"
			Grid.Column="0"
			Padding="8"
			Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
			BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
			BorderThickness="1"
			CornerRadius="12">
			<ListView
				x:Name="CategoryList"
				ItemsSource="{x:Bind Categories}"
				ItemTemplate="{StaticResource SettingsCategoryTemplate}"
				SelectionMode="Single"
				SelectionChanged="CategoryList_SelectionChanged"
				HorizontalAlignment="Stretch"
				HorizontalContentAlignment="Stretch"
				VerticalAlignment="Stretch"
				MinWidth="220"
				TabIndex="0">
				<ListView.Resources>
					<Style TargetType="ListViewItem" BasedOn="{StaticResource ListViewItemRevealStyle}">
						<Setter Property="HorizontalContentAlignment" Value="Stretch" />
					</Style>
				</ListView.Resources>
			</ListView>
		</Border>

		<Border
			Grid.Row="1"
			Grid.Column="1"
			Padding="24"
			Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
			BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
			BorderThickness="1"
			CornerRadius="12">
			<StackPanel x:Name="CategoryDetails" Spacing="12">
				<TextBlock
					x:Name="CategoryTitle"
					Text="Select a category"
					Style="{StaticResource SubtitleTextBlockStyle}" />
				<TextBlock
					x:Name="CategorySummary"
					Text="Choose a settings category from the list to preview its configuration area."
					TextWrapping="Wrap"
					Style="{StaticResource BodyTextBlockStyle}"
					Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
				<TextBlock
					Text="Detailed controls for this category will appear here in a future update."
					TextWrapping="Wrap"
					Style="{StaticResource CaptionTextBlockStyle}"
					Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
			</StackPanel>
		</Border>
	</Grid>
</ContentDialog>
