<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Mutation.Ui.Views.PromptEditorWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Edit Prompt">

    <Grid Padding="24" RowSpacing="16" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Edit Prompt Configuration" Style="{StaticResource TitleTextBlockStyle}" />

        <Grid Grid.Row="1" ColumnSpacing="16" RowSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBox x:Name="TxtName" Header="Name" PlaceholderText="e.g. Summarize" Grid.Column="0" />
            <TextBox x:Name="TxtHotkey" Header="Hotkey" PlaceholderText="e.g. CTRL+ALT+S" Grid.Column="1" />
            
            <CheckBox x:Name="ChkAutoRun" Content="Run automatically after transcription" Grid.Row="1" Grid.ColumnSpan="2" />
        </Grid>

        <TextBox x:Name="TxtContent" 
                 Grid.Row="2" 
                 Header="Prompt Instructions" 
                 PlaceholderText="Enter system instructions for the LLM..." 
                 AcceptsReturn="True" 
                 TextWrapping="Wrap" 
                 ScrollViewer.VerticalScrollBarVisibility="Auto" />

        <StackPanel Orientation="Horizontal" Grid.Row="3" Spacing="12" HorizontalAlignment="Right">
            <Button x:Name="BtnTest" Content="Test Run" Click="BtnTest_Click" ToolTipService.ToolTip="Run the prompt against current clipboard content" />
            <Button x:Name="BtnSave" Content="Save" Style="{StaticResource AccentButtonStyle}" Click="BtnSave_Click" />
            <Button x:Name="BtnCancel" Content="Cancel" Click="BtnCancel_Click" />
        </StackPanel>
    </Grid>
</Window>
